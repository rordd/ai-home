# AI홈 설계 문서

## 프로젝트 구조
```
ai-home/
├── index.html          # TV 대시보드 UI (프론트엔드)
├── server/
│   ├── server.js       # Express API 서버
│   └── package.json
├── skills/
│   ├── fridge.js       # 냉장고 스킬 (JSON DB CRUD)
│   ├── washer.js       # 세탁기 목업
│   ├── aircon.js       # 에어컨 목업
│   ├── vacuum.js       # 로봇청소기 목업
│   └── lights.js       # 조명 목업
├── data/
│   ├── fridge.json     # 냉장고 재고 데이터
│   └── appliances.json # 가전 상태 데이터
└── DESIGN.md
```

## API 서버 (server/server.js)
- Express.js, 포트 3000
- CORS 허용 (GitHub Pages에서 접근)
- 엔드포인트:
  - `POST /api/chat` → { message: string } → picoclaw agent CLI 파이프 → { reply: string }
  - `GET /api/fridge` → 냉장고 재고 조회
  - `POST /api/fridge` → 냉장고 아이템 추가/수정/삭제
  - `GET /api/appliances` → 가전 상태 조회
  - `POST /api/appliances/:device` → 가전 제어 (on/off/설정 변경)

## TV 대시보드 UI (index.html)
- 단일 HTML 파일 (CSS/JS 인라인)
- 다크 테마, TV 최적화 (큰 글씨, 큰 버튼)
- 레이아웃:
  ```
  ┌─────────────────────────────────────┐
  │  🐦 빈버드 AI홈        ⛅ 22° 🕐  │
  ├──────────────┬──────────────────────┤
  │              │  🧊 냉장고 재고      │
  │   💬 채팅    │  계란 10개           │
  │              │  우유 1L (D-3)       │
  │  빈버드와    │  ...                 │
  │  대화하기    ├──────────────────────┤
  │              │  🏠 가전 상태        │
  │              │  세탁기: 대기        │
  │              │  에어컨: 꺼짐        │
  │              │  조명: 거실 ON       │
  │              │  청소기: 충전중      │
  └──────────────┴──────────────────────┘
  ```

## 데이터 형식

### fridge.json
```json
{
  "items": [
    {"id": 1, "name": "계란", "quantity": "10개", "expiry": "2026-02-25", "category": "유제품/난류"}
  ],
  "lastUpdated": "ISO8601"
}
```

### appliances.json
```json
{
  "washer": {"status": "idle|running|done", "course": null, "remainingMin": 0},
  "aircon": {"status": "on|off", "targetTemp": 24, "currentTemp": 22, "mode": "auto|cool|heat"},
  "vacuum": {"status": "docked|cleaning|returning", "battery": 100},
  "lights": {"거실": {"on": true, "brightness": 80}, "안방": {"on": false, "brightness": 0}, ...}
}
```

## PicoClaw 연동
- `echo "메시지" | HOME=/home/rordd/.picoclaw-home picoclaw agent` 로 대화
- 응답에서 이모지 프리픽스 라인만 추출하거나 전체 stdout 사용
- 빈버드 홈: `/home/rordd/.picoclaw-home`
